---
- name: Deploy Container docker
  hosts: localhost
  become: true

  tasks:
    - name: deploy application
      command: sudo docker run --name edureka-project1-application -itd -p 8095:80 llorenckaferocks/edureka-project1:1.0